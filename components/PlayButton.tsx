interface PlayButtonProps {
  variant?: any
  size?: any
}

export default function PlayButton({ variant, size }: PlayButtonProps) {
  return (
    <div className="">
      {/* PlayButton component content */}
    </div>
  )
}